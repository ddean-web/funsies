import { FeaturedGamesPipe } from './featured-games.pipe';

describe('FeaturedGamesPipe', () => {
  it('create an instance', () => {
    const pipe = new FeaturedGamesPipe();
    expect(pipe).toBeTruthy();
  });
});
